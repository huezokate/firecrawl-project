---
id: T-001-01
story: S-001
title: project-scaffold
type: task
status: open
priority: high
phase: ready
depends_on: []
---

## Context

Initialize the repository structure for the full-stack app. This ticket creates the skeleton that all subsequent tickets build on top of — backend Python package, React frontend, shared config files, and env var setup.

## Acceptance Criteria

- `backend/` directory with `main.py`, `requirements.txt`, `.env.example`
- `frontend/` directory initialized with Vite + React + TypeScript
- Root `.gitignore` covering Python, Node, and env files
- `README.md` with project overview and local dev instructions
- `CLAUDE.md` updated with project type and description
- Git initialized with initial commit
