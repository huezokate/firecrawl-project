---
id: T-001-06
story: S-001
title: deployment-config
type: task
status: open
priority: high
phase: ready
depends_on: [T-001-04, T-001-05]
---

## Context

Add all deployment configuration so the app is ready to ship. Frontend goes to Cloudflare Pages, backend goes to Railway. This ticket adds config files only — no code logic changes.

## Acceptance Criteria

- `frontend/wrangler.toml` configured for Cloudflare Pages deployment of the Vite build output
- `frontend/_redirects` file: `/* /index.html 200` for SPA routing
- `Procfile` in `backend/` for Railway: `web: uvicorn main:app --host 0.0.0.0 --port $PORT`
- `railway.toml` in `backend/` with build and start commands
- `backend/requirements.txt` complete and pinned
- `frontend/package.json` has `build` script outputting to `dist/`
- Root `README.md` updated with deploy instructions for both platforms
- `.env.example` in `backend/` documents all required env vars with placeholder values
